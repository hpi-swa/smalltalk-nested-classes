nextChainAndIndex	| tChain tIndex size |	tChain := chain.	tIndex := index.	size := self class outer class defaultSize.	tIndex := tIndex + 1.	(tChain == 0 or: [ tIndex > (self class outer chainAt: tChain) size ])		ifTrue: [ "find next chain" 			 [ tChain := tChain + 1 ]				doWhileTrue: [ (self class outer chainAt: tChain) size == 0					& (tChain < size) ].			tIndex := 1 ].	tChain >= size		ifTrue: [ ^ nil ]		ifFalse: [ ^ { tChain. tIndex }].